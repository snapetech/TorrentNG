# Copy to /etc/rtorrentng-prod/secrets.env on the host and chmod 0600.
# Keep the populated file out of git and rotate values that were previously
# embedded in a unit or checked into a repository.
RTNG_SECRET_KEY=replace-with-a-random-32-byte-hex-value
RTNG_API_TOKENS=replace-with-a-random-automation-token
