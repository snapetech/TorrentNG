name: Windows Smoke

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "sidecar/**"
      - "webui/**"
      - ".github/workflows/windows-smoke.yml"

permissions:
  contents: read

jobs:
  smoke:
    timeout-minutes: 30
    runs-on: [self-hosted, Windows, X64, packer-windows]
    defaults:
      run:
        shell: powershell
    steps:
      - uses: actions/checkout@v7

      - name: Select Rust toolchain
        run: rustup default stable

      - uses: actions/setup-node@v7
        with:
          node-version: "22"

      - name: Build sidecar
        run: cargo build --locked --manifest-path sidecar/Cargo.toml

      - name: Test sidecar
        run: cargo test --locked --manifest-path sidecar/Cargo.toml

      - name: Build webui
        working-directory: webui
        run: |
          npm.cmd ci
          npm.cmd run build
