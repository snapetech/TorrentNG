## Release note

Choose one:

- [ ] User-facing change: add one validated fragment under `release-notes/`.
- [ ] Internal-only change: add the exact marker as a separate line in this description.

<!-- For internal-only work, replace this comment with: release-note: none -->

The release-note validation workflow checks the selected option and previews the fragment.
